id: tradeWords                 # 기능 ID (POST /ask에서 feature_id로 사용)
name: Trade Terms Bot
models:
  llm: gpt-3.5-turbo
  embedding: text-embedding-3-large
retriever:
  k: 4
paths:
  # 기존 DB를 그대로 쓰고 싶다면, 아래를 네가 쓰던 경로로 변경:
  # chroma_dir: ./ChromaDB/DB_tradeWords
  chroma_dir: features/tradeWords/chromadb
  system_prompt: features/tradeWords/prompts/system.txt
  datasets_dir: features/tradeWords/datasets
collection_name: tradeWords
